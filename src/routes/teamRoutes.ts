import express from "express";
import { TeamController } from "../controllers/teamController";

const router = express.Router()

const teamController = TeamController;

// router.route('/').get(teamController.)

export default router;